services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    shm_size: '2gb'
    volumes:
      - .:/workspace
    environment:
      - HF_HOME=/workspace/.hf_cache
      - EMBEDDING_SERVER_URL=http://embed-server:8765
    working_dir: /workspace

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 10s
      timeout: 5s
      retries: 5

  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "bash -c ':>/dev/tcp/localhost/6333' 2>/dev/null"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

  embed-server:
    build:
      context: .
      dockerfile: 5-embed/docker/Dockerfile
    ports:
      - "8765:8765"
    depends_on:
      qdrant:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8765/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 90s

volumes:
  ollama_models:
  qdrant_storage:
